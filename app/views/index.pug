//- extends layout.pug

block scripts

block content
    | My build group list comes here! But only if more than one build group is set up! Compatibility!
    ul
      each config, index in configs
        li
          a(href=`monitor/${index}`) Monitor #{index}
